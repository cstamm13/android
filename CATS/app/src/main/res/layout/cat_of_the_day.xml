<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:theme="@android:style/Theme.Material"
    tools:context=".CatOfTheDayAdapter">

    <ImageView
        android:id="@+id/cat_of_the_day_photo"
        android:contentDescription="@string/cat_of_the_day"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:clickable="true"
        android:fadeScrollbars="true"
        android:footerDividersEnabled="true"
        android:foregroundGravity="center"
        android:scrollbars="vertical"
        android:scrollingCache="true"
        android:visibility="visible"/>

    <Button
        android:id="@+id/continue_selection"
        style="@android:style/TextAppearance.Material.Widget.Button"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_weight="1"
        android:text="@string/view_on_site"
        android:gravity="center"
        android:layout_margin="@dimen/activity_vertical_margin" />

</LinearLayout>